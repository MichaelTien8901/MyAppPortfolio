<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>
    <style name = "AppButton" parent="AppTheme">
        <item name="android:layout_width">193dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:textColor">@android:drawable/screen_background_light</item>
    </style>
    <style name="ButtonOrange" parent="AppButton">
        <item name="android:background">#f08c35</item>
    </style>
    <style name="ButtonRed" parent="AppButton">
        <item name="android:background">#ff053e</item>
    </style>
</resources>
